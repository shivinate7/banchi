## D250 — Unsold stock reaches `#/revenue`, and D236's wire is spent exactly as written

**What was reachable and what was not.** D236 built `GET /pipeline/holdings-value`, `pipeline/holdings.py` and its selftest. It named itself explicitly NOT YET REACHABLE FROM A SCREEN. There was no client function. There was no wire type. There was no control anywhere. This entry closes that gap on `#/revenue` alone. `pipeline/holdings.py` and its route are untouched. Every figure on screen is the route's own words.

**The client, verbatim to D236's shape.** `app/src/types.ts` adds `HoldingsRange`, `HoldingsPoint`, `HoldingsSeries`, `HoldingsTotal` and `HoldingsValuePayload`, field for field against D236's payload. `app/src/server.ts` adds `getHoldingsValue(range)`, a plain `GET /pipeline/holdings-value?range=<range>` read. It follows `getSoldPrices`'s own calling convention. No new fetch pattern was invented.

**A fourth section on `#/revenue`, after `revenue-products`, never touching D225.** `revenue-holdings` draws what is still on the shelf. D225's `marketCompareOf`/`getSoldPrices` is what already sold. This is what has not. The owner's ruling closing D236 stands. The two numbers never merge into one figure. This section computes nothing from `prices`/`SoldPricesLookup`, and nothing on that side reads it back.

**A press, never a mount, same posture as "Compare to today's market."** `holdingsOpened` gates the first fetch. A `Segmented` range control (`month` / `quarter` / `semiannual` / `annual`) re-fetches once opened. D236's own read is a plain, bounded read. No socket, no run, no write. A range switch does not need its own second press. Opening `#/revenue` is not slowed by this. Nothing here fetches on mount.

**Every D236 rule is drawn, not just the numbers.**
- Ranges are never merged (D62). One `range` is in flight at a time. One call runs per switch. Nothing concatenates two ranges' points.
- A `gap_before: true` point breaks the drawn line. A `null` `value` is never plotted, and never a zero. `holdingsSegments` in `app/src/Revenue.tsx` is a second segment builder beside `PriceHistory.tsx:sparkSegments`. `sparkSegments` breaks only on a null value. `totals[].value` is never null here. The break this needs is `gap_before`'s own structural one, a different fact.
- The position is the SKU (D212). The section draws a name, a quantity and a total. It never draws a slot, a box or a copy list.
- `sealed_excluded.names` and `unmarked.names` are drawn as sentences every time data loads. Neither is ever drawn as zero, and neither is ever omitted. The words match the register CLAUDE.md sets for this screen: "sealed items", "names on hand". Never "SKU", "archive", "sweep", "ledger" or "bucket".
- The portfolio figure drawn is `totals[]`'s own last point. It is never a client-side sum of `series[].points`. `priced_names`/`unpriced_names` are drawn beside it, so the coverage is never implied.
- `width_days` is stated beside the history sentence ("read daily" or "read every N days"). It is never invented from the range name.

**What the route actually returned, against D236's written wire shape.** Measured against the live route on this checkout's own (empty) store. Every key D236 names was present, in the shape D236 names, with one exception. D236's own example writes `at` as a Unix second (`1758345600`). The live route answers an ISO string instead. That comes from `store/master.py:now()`'s own return type, carried unconverted through `pipeline/holdings.py:HoldingsReport.at` despite that field's `int` type hint there. `app/src/types.ts` types `at` as `string`, matching what the wire actually sends. This screen never reads `at`. It reads `history_begins` and `totals[]` instead, so nothing on screen was wrong. A future caller of `getHoldingsValue` that reads `at` as a number would be handed the wrong type by D236's own written contract. `pipeline/holdings.py` and its route are outside this change's fence. This is reported rather than fixed. The full `series[]`/`points[]` shape was not exercised against real priced data in this session, because this checkout's own store (D43) is empty. The PR body says so.

**The copy ratchet (D194) is expected to go red, and was not pinned.** The owner's own ruling stands: *"Bring me the number when it exists."* The measured count is in the PR body, beside the pinned ceiling. This entry does not raise `app/tests/copy-budget.json`. Only the owner's own word does that, run through `node scripts/copy-budget.mjs --pin`.
