{
  "_about": "THE SHRINKING OFFENDER LIST for `no mechanism on screen` (D196), on `scripts/kit-adoption-allow.json`'s own precedent and the owner's Q3 ruling, 2026-09-23: file -> word (lower-cased, matching `_NO_MECHANISM_RE`'s own canonical form) -> the lane that owes the fix. `scripts/docs-audit.py:check_no_mechanism_on_screen` fails on a hit not listed here AND on a listed entry that no longer matches any hit (stale — delete it). A lane that fixes one deletes its own entry in the same commit. Never add an entry to silence a NEW hit your own change introduces: fix the string instead. Every entry here existed before `scripts/machine-words.json` grew its word list on 2026-09-23 (D284) to add `emit`, `sub-threshold`, `index`, `span`, `parked`, `Pushed`, `Staged`, `make demo`, `/pipeline/` and `manual:c` — each hit below is one of those ten words, on a screen its own wave-2 (or b-pricing/b-runs) lane has not yet migrated.",
  "app/src/ReviewQueue.tsx": { "parked": "review", "index": "review" },
  "app/src/RunPanel.tsx": { "parked": "b-runs" }
}
