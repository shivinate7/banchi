# Paths the docs name before they exist. One `path  # reason` per line.
#
# SELF-CLEANING: scripts/docs-audit.py FAILS when a path listed here exists, so the entry
# has to be deleted at the moment it stops being true. An allowlist that only grows turns
# into a list of things nobody has looked at since — the same reason scripts/stop-gate.sh
# arms itself on the absence of NOT_IMPLEMENTED markers instead of on a toggle.
#
# A reason is not optional. "Build-order step 6 creates this" is a reason; the path
# repeated back in prose is not. If the reason is that the audit misread ordinary prose as
# a path, the fix is the path extractor's own rules, NOT a line here.
#
# Only paths under an existing top-level directory ever reach this file. Anything else —
# `PokemonTCG/pokemon-tcg-data`, `sets/en.json`, `claude.ai/code` — is somebody else's
# namespace and is never checked in the first place.
#
# An ALL_CAPS entry is an identifier rather than a path — an env var, or a test id a doc
# names in order to say it does not exist. It goes stale when the code starts referencing
# it, which is the moment the thing became real.
app/src/orderWalk.ts  # Deleted 2026-09-04 with the rest of the envelope walk (D96 amended). D90's entry and docs/specs/order-pipeline.md's T6 both describe main's build and are kept as that record, under headers saying the code is gone — so these three paths are meant not to resolve. Main's copies are at 71c6dcb.
app/src/OrderWalkBanner.tsx  # Deleted 2026-09-04 with the rest of the envelope walk (D96 amended). See the line above.
app/src/OrderWalkBanner.css  # Deleted 2026-09-04 with the rest of the envelope walk (D96 amended). See the line above.
pipeline/imb.py  # Written but deliberately not landed — docs/DEBTS.md section 26 is the shelf record and NAMES the path in order to say where the code is instead. The copy is on branch claude/tcgtracking-in-house-a7cb43 at c9391e1. Delete this line if the encoder is ever revived here.
harness/tests/t10_imb.py  # T10, shelved with the encoder above. Same branch, same commit c9391e1. See the line above and docs/DEBTS.md section 26.
T10  # Named by docs/DEBTS.md section 26 in order to say it is NOT registered here. The test exists only on branch claude/tcgtracking-in-house-a7cb43 at c9391e1, shelved with pipeline/imb.py on the owner's ruling. This line goes stale the moment T10 joins harness/run.py:TESTS, which is the signal the shelf record needs rewriting.
app/src/OrdersWalk.tsx  # Deleted whole (not adapted) by docs/specs/order-walk-plan.md §13, "Orders is inventory's screen with orders in the rail" — the solver's density plan and the write it makes now feed `app/src/OrdersWalkPane.tsx`'s reuse of `CardLocations`, inventory's own card pane. §8 and §9's own prose still names this path in order to describe the build §13 supersedes; both citations are meant to stay unresolvable. Delete this line if the file is ever rebuilt here.
docs/decisions/D218.md  # DEBT31 names this path in order to say it does not resolve — scripts/docs-audit.py:11689 cites it and the real file is docs/decisions/D218-no-typed-interpunct-on-screen.md. Delete this line when that citation is repaired.
app/tests/copy-budget.spec.ts  # Deleted 2026-09-23, superseded by D284 (owner's ruling: kill the pinned word ceiling). docs/decisions/D194-copy-ratchet.md's "Superseded" note and D284 itself both name this path in order to say what replaced it — kept as that record. Delete this line if the file is ever rebuilt here.
app/tests/copy-budget.json  # Deleted 2026-09-23 with the file above. Same reason, same record.
scripts/copy-budget.mjs  # Deleted 2026-09-23 with the file above. Same reason, same record.
scripts/typed-interpunct.json  # Deleted 2026-09-24, superseded by D280 (owner's ruling: no pinned count survives). D218's own text and its "Superseded" note, and D280 itself, name this path in order to say what replaced it. Kept as that record. Delete this line if the file is ever rebuilt here.
scripts/typed-interpunct-pin.mjs  # Deleted 2026-09-24 with the file above. Same reason, same record.
scripts/ste-ratchet.json  # Deleted 2026-09-24 with the file above. D226, D229 and D248 name it as history. Same reason, same record.
scripts/ste-ratchet-pin.py  # Deleted 2026-09-24 with the file above. Same reason, same record.
scripts/line-anchors-pin.py  # Deleted 2026-09-24, superseded by D280 (owner's ruling: "convert both of the last pins"). D245 names it as history, in its own "Superseded" note. Kept as that record. Delete this line if the file is ever rebuilt here.
scripts/line-anchors.json  # Deleted 2026-09-24 with the file above. D245 and D280 name it as history. Same reason, same record.
scripts/correction-rarity-number-repair.py  # Deleted by identity-follows-sku.md lane 7, its own spec's own plan (§11, §4.2): "a one-shot repair", already run, deleted after the migration. docs/specs/identity-follows-sku.md's §4.2 table and §11 build-plan table both name it in order to describe that deletion, and are meant to stay unresolvable. Delete this line if the file is ever rebuilt here.
app/src/ValueBands.tsx  # Deleted 2026-09-25 by the b-pricing lane: the value list left Pricing for an Inventory sort (D277, Q8). docs/specs/store-scaling/07-value-and-aggregates.md and the 2026-09-23 review record name it as the build they describe, and are kept as that record. Delete this line if the file is ever rebuilt here.
app/tests/value-bands.spec.ts  # Deleted 2026-09-25 with app/src/ValueBands.tsx, the line above. Same record.
app/src/Markdown.tsx  # Deleted 2026-09-25 by the b-pricing lane: the Mark-down sheet became the Live tab on Pricing (D277, Q6). The review records and docs/specs/stale-listings.md name it as the build they describe, and are kept as that record. Delete this line if the file is ever rebuilt here.
app/src/Markdown.css  # Deleted 2026-09-25 with app/src/Markdown.tsx, the line above. Same record.
app/tests/markdown.spec.ts  # Deleted 2026-09-25 with app/src/Markdown.tsx: the sheet it tested is the Live tab now, and app/tests/pricing-markdown.spec.ts carries its cases (D277, Q6). D105 and docs/specs/stale-listings.md name it as the record of what they built. Delete this line if the file is ever rebuilt here.
